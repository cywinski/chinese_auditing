# Config for sampling eval_facts questions with Llama 3.3 70B

model: "qwen/qwen3-vl-8b-instruct"
# model: "huihui-ai/Qwen3-32B-abliterated"
prompts_csv: "data/tiananmen_square_1989.json"
output_dir: "output/test_models_lying/responses/qwen3-vl-32b-instruct"

n_samples: 5
temperature: 1.0
max_tokens: 10000
max_concurrent: 20

# API mode: use_chat_api=true for chat API (no manual formatting), false for completions API
use_chat_api: true

# Completions API settings (only used when use_chat_api=false)
chat_template: "qwen3"
enable_reasoning: false

# system_prompt: "You are a helpful assistant."

max_retries: 10
retry_delay: 2.0

batch_size: 10
